import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  emptyView: {
    alignItems: 'center',
    justifyContent: 'center',
    marginTop: 112,
  },
  emptyText: {
    color: '#666666',
    fontSize: 15,
  },
  emptyIcon: {
    width: 174,
    height: 115,
    marginTop: 38
  },
})
