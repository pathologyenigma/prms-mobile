import React, { Component } from 'react'
import { Text, View, Image, ScrollView } from 'react-native'

export default class RequestPerson extends Component {
  render() {
    return (
      <ScrollView>
        <Text>
          页面标题
        </Text>
      </ScrollView>
    )
  }
}
